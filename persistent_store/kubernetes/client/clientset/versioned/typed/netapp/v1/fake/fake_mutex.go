// Copyright 2018 NetApp, Inc. All Rights Reserved.
// Code generated by client-gen. DO NOT EDIT.

package fake

import (
	netappv1 "github.com/netapp/trident/persistent_store/kubernetes/apis/netapp/v1"
	v1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	labels "k8s.io/apimachinery/pkg/labels"
	schema "k8s.io/apimachinery/pkg/runtime/schema"
	types "k8s.io/apimachinery/pkg/types"
	watch "k8s.io/apimachinery/pkg/watch"
	testing "k8s.io/client-go/testing"
)

// FakeMutexes implements MutexInterface
type FakeMutexes struct {
	Fake *FakeTridentV1
}

var mutexesResource = schema.GroupVersionResource{Group: "trident.netapp.io", Version: "v1", Resource: "mutexes"}

var mutexesKind = schema.GroupVersionKind{Group: "trident.netapp.io", Version: "v1", Kind: "Mutex"}

// Get takes name of the mutex, and returns the corresponding mutex object, and an error if there is any.
func (c *FakeMutexes) Get(name string, options v1.GetOptions) (result *netappv1.Mutex, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootGetAction(mutexesResource, name), &netappv1.Mutex{})
	if obj == nil {
		return nil, err
	}
	return obj.(*netappv1.Mutex), err
}

// List takes label and field selectors, and returns the list of Mutexes that match those selectors.
func (c *FakeMutexes) List(opts v1.ListOptions) (result *netappv1.MutexList, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootListAction(mutexesResource, mutexesKind, opts), &netappv1.MutexList{})
	if obj == nil {
		return nil, err
	}

	label, _, _ := testing.ExtractFromListOptions(opts)
	if label == nil {
		label = labels.Everything()
	}
	list := &netappv1.MutexList{}
	for _, item := range obj.(*netappv1.MutexList).Items {
		if label.Matches(labels.Set(item.Labels)) {
			list.Items = append(list.Items, item)
		}
	}
	return list, err
}

// Watch returns a watch.Interface that watches the requested mutexes.
func (c *FakeMutexes) Watch(opts v1.ListOptions) (watch.Interface, error) {
	return c.Fake.
		InvokesWatch(testing.NewRootWatchAction(mutexesResource, opts))
}

// Create takes the representation of a mutex and creates it.  Returns the server's representation of the mutex, and an error, if there is any.
func (c *FakeMutexes) Create(mutex *netappv1.Mutex) (result *netappv1.Mutex, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootCreateAction(mutexesResource, mutex), &netappv1.Mutex{})
	if obj == nil {
		return nil, err
	}
	return obj.(*netappv1.Mutex), err
}

// Update takes the representation of a mutex and updates it. Returns the server's representation of the mutex, and an error, if there is any.
func (c *FakeMutexes) Update(mutex *netappv1.Mutex) (result *netappv1.Mutex, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootUpdateAction(mutexesResource, mutex), &netappv1.Mutex{})
	if obj == nil {
		return nil, err
	}
	return obj.(*netappv1.Mutex), err
}

// Delete takes name of the mutex and deletes it. Returns an error if one occurs.
func (c *FakeMutexes) Delete(name string, options *v1.DeleteOptions) error {
	_, err := c.Fake.
		Invokes(testing.NewRootDeleteAction(mutexesResource, name), &netappv1.Mutex{})
	return err
}

// DeleteCollection deletes a collection of objects.
func (c *FakeMutexes) DeleteCollection(options *v1.DeleteOptions, listOptions v1.ListOptions) error {
	action := testing.NewRootDeleteCollectionAction(mutexesResource, listOptions)

	_, err := c.Fake.Invokes(action, &netappv1.MutexList{})
	return err
}

// Patch applies the patch and returns the patched mutex.
func (c *FakeMutexes) Patch(name string, pt types.PatchType, data []byte, subresources ...string) (result *netappv1.Mutex, err error) {
	obj, err := c.Fake.
		Invokes(testing.NewRootPatchSubresourceAction(mutexesResource, name, data, subresources...), &netappv1.Mutex{})
	if obj == nil {
		return nil, err
	}
	return obj.(*netappv1.Mutex), err
}
